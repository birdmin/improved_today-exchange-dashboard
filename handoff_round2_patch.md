# 인수인계 문서 (일곱 칸) — improved_today-exchange-dashboard [라운드2-패치]

> 라운드2(통화쌍 선택 UI)의 실사용 검증 중, "어제 데이터"를 가져오는 로직이 애초에(과제4 원본부터)
> 죽은 코드였다는 근본 결함이 발견되어 패치했습니다. 통화쌍 UI 자체나 검사 10개 정의는 바뀌지
> 않았습니다 — TC10이 원래 요구하던 조건을 이제야 실제로 만족시키게 된 것입니다.

---

## 1. 목표
카드 상단에 **비교 통화쌍 선택 UI**(달러↔원 / 원↔엔 / 엔↔달러)를 추가하고, 선택한 쌍에 따라
요약값·계산기·히스토리 표(변동률 포함)가 전부 그 쌍 기준으로 전환되도록 재설계한다.
계산기는 통화 1개만 고르면 나머지 통화로 자동 환산되어야 한다(2개 다 고르는 방식 금지).
또한 브라우저에 라운드1의 옛날 기록(localStorage)이 남아있는 환경, 완전히 새 환경(시크릿 탭)
양쪽 모두에서 어제 대비 변동률이 정상 계산되어야 한다.

**[패치 추가] "어제 데이터" 조회 자체가 과제4 원본부터 실제로 동작한 적 없는 코드였음이
드러나, 실제로 동작하는 API로 교체했다. 이 패치의 목표는 TC01~TC10 중 어느 것도 새로
추가하거나 바꾸는 것이 아니라, 기존 TC10("새 환경에서도 오류 없이 변동률 계산")이
실제로 세 통화쌍 모두에서 참이 되도록 만드는 것이다.**

## 2. 현재 상태
- 저장소: https://github.com/birdmin/improved_today-exchange-dashboard
- 배포(GitHub Pages) 주소: https://birdmin.github.io/improved_today-exchange-dashboard/
- 저장소 버전 ID(git commit hash): `b7f0fad71dfa4177291f419157f31ee81f96752b`
  이전 커밋 `d2f9dfa3f50411738e9e4cad3cbee0e42e176453`에서 "어제 데이터 조회" 로직만 교체된 버전.
  ⚠️ 검증 상태: AI A는 이전 라운드들과 같은 이유(GitHub 웹 캐시)로 이 커밋 내용을 직접
  대조하지 못했다. AI B가 저장소를 열었을 때 아래 "3. 실행 명령"의 화면·동작과 실제로
  일치하는지부터 확인할 것.
- ⚠️ **이전 AI B 검증 결과 무효 처리**: 라운드2에서 AI B가 보고한 "TC10 PASS"는 코드를
  읽기만 하는 정적 검토였고, 실제로는 존재하지 않는 API 엔드포인트(`open.er-api.com/v6/history/...`)를
  호출하고 있어 원↔엔·엔↔달러 변동률이 실제로는 계산되지 않는 결함을 놓쳤다. 이번 재검증에서는
  가능하면 코드를 실제로 실행(또는 최소한 fetch 대상 URL이 실존하는지 확인)해서 같은 종류의
  맹점을 반복하지 않도록 할 것.
- 패치 내용: "어제 데이터" 백필을 Frankfurter(ECB 공식, 무료·무인증, KRW·JPY 모두 지원)로 교체,
  하드코딩 가짜값(`1380.502`) 완전 제거. 오늘 실시간 값은 기존과 동일하게 open.er-api.com 사용.

## 3. 실행 명령
```
mkdir fresh_check
cp index.html fresh_check/
cd fresh_check
# 브라우저로 index.html을 연다
```
- 필요 버전: 현대 브라우저 1개, 설치/키 불필요 (open.er-api.com 무료·무인증)
- 확인 순서:
  1. 로드 직후 카드 우측 상단에 "비교 통화쌍" 선택란이 보이고 기본값이 "달러 ↔ 원"인지 확인
  2. 선택란에 "원 ↔ 엔", "엔 ↔ 달러"로 바꿔가며 요약값·계산기·히스토리 표가 함께 바뀌는지 확인
  3. 계산기에서 통화 하나만 선택해 금액을 입력하고, 반대쪽 통화로 자동 환산되는지 확인
  4. 하단 5개 장애 버튼, 특히 "4. OFFLINE" 클릭 후에도 선택된 쌍의 값이 사라지지 않는지 확인
  5. **[패치 추가]** 가능하면 브라우저 개발자도구 네트워크 탭으로 `api.frankfurter.dev` 요청이
     실제로 200 응답을 받는지, 세 통화쌍 모두 "어제 대비 변화"가 빈 값('-')이 아닌지 확인

## 4. 통과 검사 (10개)
공통 사용 상한: 시간 45분 / 도구·호출 15회 (AI A·AI B 동일 적용)

| ID | 입력 | 기대값 | 이전 결과(무효화됨) |
|---|---|---|---|
| TC01 | 최초 로드 | 통화쌍 기본값 = "달러 ↔ 원" | PASS(재검증 불필요, 이번 패치와 무관) |
| TC02 | 통화쌍 선택 UI 확인 | 정확히 3개 옵션(달러↔원/원↔엔/엔↔달러) 존재 | PASS(재검증 불필요) |
| TC03 | "원 ↔ 엔" 선택 | 요약값 2개가 "1,000원→?엔", "100엔→?원"으로 전환 | PASS(재검증 불필요) |
| TC04 | "엔 ↔ 달러" 선택 | 요약값 2개가 엔·달러 기준으로 전환 | PASS(재검증 불필요) |
| TC05 | 아무 통화쌍에서 계산기 확인 | 선택지가 그 쌍의 두 통화만 존재, 하나 고르면 반대쪽으로 자동 환산 | PASS(재검증 불필요) |
| TC06 | 통화쌍 전환 후 히스토리 표 확인 | 표 라벨("환율(1X당 Y)")과 부제가 선택된 쌍 기준으로 바뀜 | PASS(재검증 불필요) |
| TC07 | 통화쌍 전환 후 변동률 확인 | "어제 대비 변화"·"%"가 쌍마다 서로 다른 고유 값으로 재계산 | **재검증 필요** — 이전엔 원↔엔·엔↔달러가 실제로는 계산 안 됐음 |
| TC08 | 계산기 입력 "-1" 또는 "abc" | 조용한 처리 대신 명시적 오류 문구 | PASS(재검증 불필요) |
| TC09 | "4. OFFLINE" 클릭 | 선택된 쌍의 값이 "오래된 값 보존" 상태로 사라지지 않고 유지 | PASS(재검증 불필요) |
| TC10 | 라운드1의 옛 localStorage 기록이 남아있는 브라우저에서 새로고침, 또는 완전히 새 환경(시크릿 탭)에서 접속 | 두 경우 모두 "기록이 없습니다" 오류 없이 어제 대비 변동률이 정상 계산됨 | **재검증 필요 — 핵심 패치 대상.** 이전 PASS 판정은 정적 코드 검토만으로 나온 오판정 |

## 5. 남은 문제
1. ~~버전 ID 미검증~~ → 사용자가 배포 페이지 육안 확인 완료(해결됨).
2. ~~소수점 반올림 표시 오류(경미)~~ → 그대로 존재하나 공식 검사 대상 아님(참고용, 미해결).
3. **[신규, 패치로 발견]** "어제 데이터"를 못 가져오던 근본 원인은 애초에 존재하지 않는
   API 엔드포인트를 호출하고 있었기 때문이었음 (과제4 원본부터 있던 결함). Frankfurter로 교체해
   해결했으나, **오늘 값(open.er-api.com)과 어제 값(Frankfurter/ECB)이 서로 다른 두 기관의
   데이터**라는 미세한 비일관성이 남아있음 — 두 기관의 고시 시각·산출 방식이 달라 아주 작은
   오차가 생길 수 있음. 완전히 해소하려면 향후 단일 출처로 통일하는 것을 고려할 것.
4. **[신규]** 이전 라운드2 AI B 검증(TC10 PASS 판정)이 정적 코드 검토만으로 이뤄져 이 결함을
   못 잡았음 — 코드가 "그럴듯해 보이는 로직"을 담고 있어도 실제 실행 없이는 죽은 엔드포인트를
   구분할 수 없다는 걸 보여주는 사례. AI B는 이번엔 가능하면 실제 실행 확인을 시도할 것.

## 6. 다음 행동
1. 저장소를 직접 열어(또는 가능하면 실제 실행해) 2항의 커밋 내용이 실제로 패치 내용을 담고 있는지 확인.
2. TC07·TC10을 최우선으로 재검증 — 세 통화쌍(달러↔원/원↔엔/엔↔달러) 전부 변동률이 실제 계산되는지.
3. 나머지 TC01~06, TC08~09는 라운드2와 동일 로직이므로 간단 재확인만으로 충분.
4. 불일치하거나 안 되는 부분이 있으면 추측하지 말고 "○○가 안 됨"으로 명시.
5. 결과를 이름 가린 측정표로 정리해 최종 공개 비교 보고서에 반영(카드5, 이미 발행된 보고서 갱신).

## 7. 건드리지 말 것 (금지 범위)
- localStorage 키 이름 `aleph_t04_daily_records` 변경 금지.
- 계산기를 다시 "출발/도착 2개 선택" 방식으로 되돌리지 말 것(이번 라운드에서 의도적으로 1개 선택 방식으로 변경함).
- 통화쌍을 3개(달러↔원/원↔엔/엔↔달러) 이외로 늘리거나 줄이지 말 것.
- 기존 5개 장애 시뮬레이션 문구·동작 변경 금지.
- 오늘 실시간 값의 API 엔드포인트(`open.er-api.com`)는 교체 금지(정상 동작 중).
- **[갱신]** "어제 데이터"의 API는 더 이상 `open.er-api.com/v6/history/...`를 쓰지 않는다
  (존재하지 않는 주소였음). 지금은 Frankfurter(`api.frankfurter.dev`)를 쓴다 —
  이걸 다시 예전 방식이나 다른 제3의 출처로 바꾸지 말 것.
- 백필 실패 시 가짜 상수값(예: 이전의 `1380.502`)을 다시 넣지 말 것 — 실패하면 정직하게 빈 값 유지.
- 검사 10개(TC01~TC10) 자체를 삭제·완화·기대값 변경하지 말 것 — 이미 고정됨.

---
### 무결성 확인용
본 문서 내용이 전달 과정에서 바뀌지 않았는지 대조할 체크섬(전달 후 계산)은 별도 안내.
